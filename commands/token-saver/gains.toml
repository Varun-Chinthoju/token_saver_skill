description = "Summarize token savings from Repomix and RTK in this repository"
prompt = """
Invoke the token-saver skill.
Autonomously run the gains summary script in the plugin's directory:
1. Locate the `token-saver` skill file `skills/token-saver/SKILL.md` in the plugin directory to find the plugin root directory.
2. Run `node <plugin-root>/bin/gains.js`.
"""
