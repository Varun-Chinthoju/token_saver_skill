description = "Set up Repomix and RTK for Google Antigravity in this repository"
prompt = """
Invoke the token-saver skill.
Autonomously run the setup script in the plugin's directory:
1. Locate the `token-saver` skill file `skills/token-saver/SKILL.md` in the plugin directory to find the plugin root directory.
2. Run `node <plugin-root>/bin/setup.js --agent antigravity`.
"""
